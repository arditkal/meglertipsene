@AGENTS.md

# Arbeidsregler

- Commit og push alltid direkte til `main`-branchen.
- Ikke lag egne branches eller pull requests med mindre du eksplisitt blir bedt om det.
- Etter hver endring: `git add`, `git commit`, `git push origin main`.
